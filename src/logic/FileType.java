package logic;

// FileType is enum which contains all file types with extensions
public enum FileType {PNG("png"),GIF("gif"),TIFF("tif"),BMP("bmp"), ZIP("zip"), RAR("rar"), PDF("pdf"), MP3("mp3"), AVI("avi"), MKV("mkv"), FLV("flv"), WMV("wmv"), MOV("mov"), XML("xml"), JS("js"), PY("py"), C("c"), CPP("cpp"), H("h"), CS("cs"), JPG("jpg"),;
    FileType(String extension) {
    }
}
